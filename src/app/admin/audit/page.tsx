'use client';import {useEffect,useState} from 'react';export default function Audit(){const [logs,setLogs]=useState<any[]>([]),[filter,setFilter]=useState(''),[msg,setMsg]=useState('');async function load(){const r=await fetch('/api/admin/audit'+(filter?`?entityType=${encodeURIComponent(filter)}`:''));const j=await r.json();if(r.ok)setLogs(j.logs);else setMsg(j.error)}useEffect(()=>{load()},[]);return <><header className="pageHead"><div><h1>سجل التدقيق Audit Log</h1><p>من غيّر ماذا ومتى — سجل غير تشغيلي للحذف من الواجهة</p></div><button className="primary" onClick={load}>تحديث</button></header><div className="toolbar"><input placeholder="فلترة بنوع الكيان" value={filter} onChange={e=>setFilter(e.target.value)}/><button onClick={load}>بحث</button></div>{msg&&<div className="formError">{msg}</div>}<div className="panel tableWrap"><table><thead><tr><th>الوقت</th><th>المستخدم</th><th>الكيان</th><th>الإجراء</th><th>قبل/بعد</th></tr></thead><tbody>{logs.map(x=><tr key={x.id}><td>{new Date(x.createdAt).toLocaleString('ar-EG')}</td><td>{x.user.fullNameAr}<small>{x.user.username}</small></td><td>{x.entityType}<small>{x.entityId}</small></td><td>{x.action}</td><td><details><summary>عرض</summary><pre className="jsonMini">{JSON.stringify({before:x.beforeJson,after:x.afterJson},null,2)}</pre></details></td></tr>)}</tbody></table></div></>}
