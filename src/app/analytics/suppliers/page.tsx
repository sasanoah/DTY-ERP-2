'use client';
import {useEffect,useState} from 'react';
export default function SupplierScorecard(){const [rows,setRows]=useState<any[]>([]),[err,setErr]=useState('');async function load(){const r=await fetch('/api/analytics/suppliers');const j=await r.json();if(r.ok)setRows(j.suppliers);else setErr(j.error)}useEffect(()=>{load()},[]);return <><header className="pageHead"><div><h1>تقييم موردي POY</h1><p>تكلفة • جودة • Yield • OEE • التزام توريد • شروط دفع</p></div><button className="primary" onClick={load}>تحديث</button></header>{err&&<div className="formError">{err}</div>}<div className="panel"><div className="tableWrap"><table><thead><tr><th>المورد</th><th>التقييم</th><th>التكلفة</th><th>الجودة</th><th>Yield</th><th>OEE</th><th>التوريد</th><th>الشروط</th><th>متوسط landed</th><th>بيانات</th></tr></thead><tbody>{rows.map((s:any)=><tr key={s.supplierId}><td><b>{s.nameAr}</b><small>{s.code} • {s.country||'—'}</small></td><td><b className={s.overall>=80?'greenText':s.overall<60?'redText':''}>{s.overall.toFixed(1)}</b>/100</td><td>{s.cost.toFixed(0)}</td><td>{s.quality.toFixed(0)}</td><td>{s.yield.toFixed(0)}</td><td>{s.oee.toFixed(0)}</td><td>{s.delivery.toFixed(0)}</td><td>{s.terms.toFixed(0)}</td><td>{s.avgLandedCost?s.avgLandedCost.toFixed(2):'—'} ج/kg</td><td><small>{s.dataQuality.lots} Lots • {s.dataQuality.runCount} Runs • {s.dataQuality.deliverySamples} Deliveries</small></td></tr>)}</tbody></table></div></div><div className="panel"><p className="hint">الأوزان في V0.6: التكلفة 30%، الجودة 20%، أداء الـYield 20%، OEE 15%، الالتزام بالتوريد 10%، شروط الدفع 5%. التقييم يتحسن تلقائيًا كلما زادت البيانات الفعلية.</p></div></>}
