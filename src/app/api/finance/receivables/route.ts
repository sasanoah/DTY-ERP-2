import {prisma} from '@/lib/prisma';import {requirePermission,apiError} from '@/lib/rbac';
export async function GET(){try{const s=await requirePermission('finance.read');const invoices=await prisma.invoice.findMany({where:{customer:{companyId:s.companyId}},include:{customer:true,collections:true,salesOrder:true},orderBy:{dueDate:'asc'}});const now=Date.now();const rows=invoices.map(i=>({id:i.id,invoiceNo:i.invoiceNo,customer:i.customer.nameAr,invoiceDate:i.invoiceDate,dueDate:i.dueDate,total:Number(i.totalAmount),paid:Number(i.paidAmount),outstanding:Math.max(0,Number(i.totalAmount)-Number(i.paidAmount)),status:i.status,daysOverdue:Math.max(0,Math.floor((now-i.dueDate.getTime())/86400000))}));return Response.json({ok:true,rows,summary:{outstanding:rows.reduce((a,r)=>a+r.outstanding,0),overdue:rows.filter(r=>r.daysOverdue>0&&r.outstanding>0).reduce((a,r)=>a+r.outstanding,0)}});}catch(e){return apiError(e)}}
