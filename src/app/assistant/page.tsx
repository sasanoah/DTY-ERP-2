'use client';import {useState} from 'react';export default function Assistant(){const [q,setQ]=useState('ما أهم المشاكل التي تحتاج انتباهي اليوم؟'),[history,setHistory]=useState<any[]>([]),[busy,setBusy]=useState(false);async function ask(){setBusy(true);const r=await fetch('/api/assistant',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({question:q})});const j=await r.json();setHistory(h=>[...h,{q,a:r.ok?j.answer:j.error,mode:j.mode}]);setBusy(false)}return <><header className="pageHead"><div><h1>مساعد المصنع الذكي</h1><p>قراءة وتحليل فقط — لا ينفذ معاملات أو اعتمادات</p></div></header><div className="assistantWrap"><div className="panel assistantChat">{history.length===0&&<div className="hint">اسأل عن الإنتاج، الهالك، POY، العملاء أو الموردين.</div>}{history.map((x,i)=><div key={i} className="chatPair"><div className="chatUser">{x.q}</div><div className="chatAi">{x.a}<small>{x.mode==='AI'?'تحليل AI':'تحليل محلي من ERP'}</small></div></div>)}</div><div className="panel assistantComposer"><textarea value={q} onChange={e=>setQ(e.target.value)}/><button className="primary" onClick={ask} disabled={busy}>{busy?'جاري التحليل...':'اسأل'}</button></div></div></>}
