'use client';import {useEffect,useState} from 'react';
export default function Pilot(){const [d,setD]=useState<any>(null),[msg,setMsg]=useState('');async function load(){const r=await fetch('/api/pilot/readiness');const j=await r.json();if(r.ok)setD(j);else setMsg(j.error)}useEffect(()=>{load()},[]);async function testHook(){const r=await fetch('/api/integrations/outbox/dispatch?test=1',{method:'POST'});const j=await r.json();setMsg(r.ok?(j.skipped?'تم إنشاء Test Event لكن N8N_WEBHOOK_URL غير مضبوط':`تم إرسال ${j.sent} Event إلى n8n`):j.error)}if(!d)return <div className="panel">{msg||'جاري فحص جاهزية Pilot...'}</div>;return <><header className="pageHead"><div><h1>Pilot — جاهزية الماكينة</h1><p>{d.machine?.nameAr} • {d.machineCode}</p></div><span className={`pilotState ${d.ready?'ready':'notReady'}`}>{d.ready?'جاهز للتشغيل التجريبي':'غير جاهز بعد'}</span></header>{msg&&<div className="formNotice">{msg}</div>}<div className="kpiGrid"><div className="kpi"><span>Readiness Score</span><strong>{(d.score*100).toFixed(0)}%</strong><small>يشمل عناصر حرجة واختيارية</small></div><div className="kpi"><span>Critical Checks</span><strong>{d.checks.filter((x:any)=>x.critical&&x.ok).length}/{d.checks.filter((x:any)=>x.critical).length}</strong><small>كلها يجب أن تنجح</small></div><div className="kpi"><span>Pilot Machine</span><strong className="ltr">{d.machineCode}</strong><small>يمكن تغييره من ENV</small></div></div><div className="panel"><table><thead><tr><th>الفحص</th><th>الحالة</th><th>التفاصيل</th><th>الأهمية</th></tr></thead><tbody>{d.checks.map((x:any)=><tr key={x.key}><td>{x.label}</td><td><span className={`status ${x.ok?'ok':'danger'}`}>{x.ok?'جاهز':'مطلوب إجراء'}</span></td><td>{x.detail}</td><td>{x.critical?'حرج':'اختياري'}</td></tr>)}</tbody></table></div><div className="panel"><h2>ترتيب يوم الـPilot</h2><ol className="pilotSteps"><li>جرد POY Lot الذي سيُصرف على الماكينة ومطابقة QR.</li><li>اختيار أمر إنتاج Released وتشغيل وردية واحدة فقط.</li><li>تسجيل أي توقف لحظيًا من Live Downtime Timer.</li><li>إقفال الوردية بميزان كتلة صحيح ثم إصدار DTY Lot.</li><li>Quality Hold/Release ثم مراجعة Traceability.</li><li>احتساب Actual Cost وOEE ومقارنته بسجل المصنع اليدوي.</li></ol><div className="inlineActions"><button className="primary" onClick={load}>إعادة فحص الجاهزية</button><button onClick={testHook}>اختبار n8n Webhook</button></div></div></>}
