'use client';
import {Suspense,useEffect,useState} from 'react';import {useSearchParams} from 'next/navigation';
function TraceabilityContent(){const q=useSearchParams();const [lot,setLot]=useState('POY-2608-0001');const [data,setData]=useState<any>(null);const [msg,setMsg]=useState('');async function search(value=lot){const r=await fetch(`/api/traceability/lot/${encodeURIComponent(value)}`);const j=await r.json();if(r.ok){setData(j);setMsg('')}else{setData(null);setMsg(j.error)}}useEffect(()=>{const v=q.get('lot');if(v){setLot(v);search(v)}},[q]);return <><header className="pageHead"><div><h1>تتبع الـLot</h1><p>من POY إلى الماكينة وDTY والعميل والعكس</p></div></header><div className="toolbar"><input className="ltr" value={lot} onChange={e=>setLot(e.target.value)} placeholder="POY-... أو DTY-..."/><button className="primary" onClick={()=>search()}>تتبع</button></div>{msg&&<div className="panel">{msg}</div>}{data&&<div className="panel traceBox"><h2>{data.type==='POY'?'تتبع خامة POY':'تتبع منتج DTY'}</h2><pre>{JSON.stringify(data,null,2)}</pre></div>}</>}
export default function Traceability(){return <Suspense fallback={<div className="panel">جارٍ تحميل شاشة التتبع…</div>}><TraceabilityContent/></Suspense>}
