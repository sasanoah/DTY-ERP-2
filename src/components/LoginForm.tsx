'use client';
import {FormEvent,useState} from 'react'; import {useRouter} from 'next/navigation';
export function LoginForm(){const router=useRouter();const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const fd=new FormData(e.currentTarget);const r=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username:fd.get('username'),password:fd.get('password')})});const j=await r.json();setBusy(false);if(!r.ok)return setError(j.error||'تعذر تسجيل الدخول');router.push('/');router.refresh()}return <form onSubmit={submit}><label>اسم المستخدم<input name="username" autoComplete="username"/></label><label>كلمة المرور<input name="password" type="password" autoComplete="current-password"/></label><label>المصنع<select disabled><option>مصنع DTY الرئيسي — 2100</option></select></label>{error&&<div className="formError">{error}</div>}<button className="primary" disabled={busy}>{busy?'جاري الدخول...':'دخول'}</button><small>حسابات التطوير موجودة في README — غيّر كلمات المرور قبل الإنتاج.</small></form>}
