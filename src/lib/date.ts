function partsInZone(date:Date,timeZone:string){const f=new Intl.DateTimeFormat('en-US',{timeZone,year:'numeric',month:'2-digit',day:'2-digit',timeZoneName:'longOffset'});const p=Object.fromEntries(f.formatToParts(date).map(x=>[x.type,x.value]));const off=(p.timeZoneName||'GMT+00:00').replace('GMT','');return {year:p.year,month:p.month,day:p.day,offset:off==='UTC'?'+00:00':off}}
export function cairoPeriod(){const now=new Date();const z=partsInZone(now,'Africa/Cairo');const startDay=new Date(`${z.year}-${z.month}-${z.day}T00:00:00${z.offset}`);const startMonth=new Date(`${z.year}-${z.month}-01T00:00:00${z.offset}`);return {now,startDay,startMonth}}
